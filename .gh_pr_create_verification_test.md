# THROWAWAY — gh pr create verification test

This file exists only to verify `gh pr create` works after the
`sandbox.excludedCommands: ["gh *"]` fix in `.claude/settings.local.json`
(2026-07-16). The PR opened from this commit will be closed without
merging and this branch will be deleted immediately after verification.

If you are reading this in real repo history, something went wrong with
cleanup — safe to delete this file and close/revert the associated PR.
